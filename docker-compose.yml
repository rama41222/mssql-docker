version: '3.7'

services:
  mssql:
    image: microsoft/mssql-server-linux
    restart: always
    volumes:
      - mssql-volume:/var/opt/mssql
    env_file:
      - .env
    ports:
      - "1433:1433"

volumes:
  mssql-volume:
